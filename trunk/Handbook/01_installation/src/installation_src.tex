\documentclass[11pt, a4paper]{book}
% -------------------------------------------------------------------------------------------------------
% THIS DOCUMENT IS CURRENTLY BEING REFACTORED
%	Copy the contents of this file and paste into each part's main source file. This will ensure uniformity and ease of use. 
%	Be sure to also copy the 'Common' folder, as it contains the title image and source file. Below are the only things you need to modify:
%	> variable 'partTitle' on line 53; This changes the title page label
%	> variable 'versionNum' on line 54; Modify if the part has been updated
%	> The preface/summary file, line 77
%	> Content input filename on line 84;
% -------------------------------------------------------------------------------------------------------
%\pagestyle{myheadings}
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}

\usepackage[english]{babel}
\usepackage{graphicx}
\usepackage[colorlinks,hyperindex,plainpages=false,breaklinks]{hyperref}
\usepackage{amssymb}
\usepackage{wasysym} 
\usepackage{wrapfig}
\usepackage{enumerate}
\usepackage{placeins} % Necessary for \FloatBarrier
\usepackage{subfig}   % Necessary for subfloat (images next to each other)
\usepackage{color}
\usepackage[usenames,dvipsnames,svgnames]{xcolor}
\usepackage{listings}


\definecolor{codelightgray}{rgb}{0.87,0.87,0.87}

\hypersetup{colorlinks=true,% 
	linkcolor=black,%
	citecolor=red,%
	filecolor=blue,% 
	menucolor=black,% 
	pagecolor=black,%
	urlcolor=black}
	
\lstset{language=,
    keywordstyle=\color{blue},
    basicstyle=\scriptsize\ttfamily,
    showstringspaces=false,
    backgroundcolor=\color{codelightgray},
    morekeywords={SELECT,FROM,WHERE,AND,OR,EClass}
}

\newcommand{\note}[1]{\marginpar{\emph{#1}}}

\setcounter{tocdepth}{2}

\setlength{\parindent}{0pt} 
\setlength{\parskip}{0.3cm}

\graphicspath{{../../00_introduction/common/}{../installation_images/}}
 
% This should be the same for each part; Modify only the name and version number
\def\partTitle{Part I: Installation \& Set-up}
\def\versionNumber{3.4}
\input{../../00_introduction/common/titlepage}

% -- Setting the colored headings. Right(Visual) = Red; Left(Text) = Yellow/Orange--------------------------------------------------
% Maybe do this command, but define them differently?
\renewcommand{\headrulewidth}{1pt}
\renewcommand{\headrule}{{\color{red}%
\hrule width\headwidth height\headrulewidth \vskip-\headrulewidth}}
% ----------------------------------------------------------------------------------------------------------------------------------
 
\begin{document}

\frontmatter 
 
\maketitle

% Copyright notice
\input{../../00_introduction/common/copyright}
\newpage

% TOC
\tableofcontents

% Preface/Summary of section
\include{installation_summary}

% Store page counter
\newcounter{romanpages}
\setcounter{romanpages}{\value{page}}


 % ----------------------------------------------------------------------------------------------------------------------------------
 \mainmatter

% Main content matter for this part
\input{installation_content}

% References?
%

% Glossary?
%


\end{document}